<mvc:View xmlns:mvc="sap.ui.core.mvc" 
          xmlns="sap.m" 
          xmlns:f="sap.ui.layout.form"
          xmlns:t="sap.ui.table"
          xmlns:core="sap.ui.core"
          controllerName="mickey.controller.Main">


<!-- <Input id="idInp"></Input>
<Button id="idbtn" text="Click Me" press="onPress"></Button> -->

<t:Table id="" rows="{/empTab}" visibleRowCount="4" selectionMode="Single" rowSelectionChange="onSelect">
    <t:columns>
        <t:Column label="{i18n>XLBL_EMPID}">
            <t:template>
                <Text text="{empId}"/>
            </t:template>
        </t:Column>
        <t:Column >
            <t:label>
                <Text text="{i18n>XLBL_EMPNAME}"/>
            </t:label>
            <t:template>
                <Input value="{empName}"/>
            </t:template>
        </t:Column>
        <t:Column label="{i18n>XLBL_SAL}">
            <t:template>
                <Text text="{salary} {currency}"/>
            </t:template>
        </t:Column>
        <t:Column label="{i18n>XLBL_MSTAT}">
            <t:template>
                <Select selectedKey="{mStat}">
                    <items>
                        <core:Item text="Married" key="M"/>
                        <core:Item text="Single" key="S"/>
                    </items>
                </Select>
            </t:template>
        </t:Column>
        <t:Column label="{i18n>XLBL_SMK}">
            <t:template>
               <CheckBox selected="{smoker}"/>
            </t:template>
        </t:Column>
        <t:Column label="{i18n>XLBL_GND}">
            <t:template>
                <Image src="{gender}" width="30px" height="30px"/>
            </t:template>
        </t:Column>
        <t:Column label="{i18n>XLBL_RAT}">
            <t:template>
                <RatingIndicator value="{rating}"></RatingIndicator>
            </t:template>
        </t:Column>
    </t:columns>
</t:Table>

<f:SimpleForm id="idSimple">
    <f:content>
        <Label text="{i18n>XLBL_EMPID}" ></Label>
        <Input id="idEmpId" width="20%" value="{empId}"></Input>
        <Label text="{i18n>XLBL_EMPNAME}" ></Label>
        <Input id="idEmpName" width="40%" value="{path: 'empName'}"></Input>
        <Label text="{i18n>XLBL_SAL}" ></Label>
        <Input id="idSal" width="30%" value="{= ${empName} === 'Anubhav' ? 20000 : ${salary}}"></Input>
        <Label text="{i18n>XLBL_CURR}" ></Label>
        <Input id="idCurr" width="30%" value="{currency}"></Input>
        <Label/>
        <Button text="{i18n>XBUT_FF}" press="onFlip" width="30%"></Button>
    </f:content>
</f:SimpleForm>
<!-- <Button text="Default" press="onDefault"/> -->
</mvc:View>
